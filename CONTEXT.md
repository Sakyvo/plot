# Plot

Minecraft PotPvP 材质包修复工具（Tauri、仅 Windows、单文件 exe、Apache-2.0）。扫描 `resourcepacks` 目录，找出 MC 1.8 无法读取的材质包并自动修复为标准结构。

## Language

**判定基准**:
结构类（嵌套/文件夹/臃肿/正常/非法）以 MC 1.8.x（PotPvP）能否实际读取与结构形态为准；zip 内部路径大小写敏感（`Pack.mcmeta` 对 MC 无效）。**Lunar非法** 另按 `pack.mcmeta` 是否含 **Lunar 非法转义** 判定（现行 Lunar 可读性），与 MC 结构维正交，可与结构类并存于同一包。
_Avoid_: 按文件名猜测、按扩展名以外的启发式放宽判定、用旧版 Lunar 实验否定非法转义病灶、把 § 颜色当病因

**三核心文件**:
`assets` 文件夹、`pack.mcmeta`（必需）、`pack.png`（可选），位于包的根层级。MC 1.8 读取一个包所依赖的全部结构。

**包（待分类对象）**:
`resourcepacks` 目录的一个顶层条目（文件或文件夹）。仅顶层条目参与分类；顶层文件夹的内部内容属于该包的内部结构，不是独立的包。

**臃肿（紫）**:
MC 能读、但含冗余内容的包。纯结构判定，不设尺寸阈值。触发条件：①根层有三核心外的多余条目 ②命中死路径黑名单 ③pack.png 仅大小写/typo 错误（可改名拯救）。垃圾文件不触发分类。
_Avoid_: 按文件大小判定臃肿、按垃圾文件判定臃肿

**垃圾文件**:
操作系统/压缩工具产生的已知无用文件：`__MACOSX`、`.DS_Store`、`Thumbs.db`、`desktop.ini`。不触发任何分类（实测 85% 流通包内含，作信号则分类失效）；包因其他原因进入处理流程时顺手删除；出现在 resourcepacks 顶层时完全忽略。

**死路径**:
MC 任何版本都不会读取的 assets 内路径，代码内维护黑名单。v1 仅收 `assets/*/records/`（Yokabi 病：音乐碟塞错位置，正版路径是 `assets/*/sounds/records/`）。1.9+ 目录（advancements/recipes 等）不列入——双版本通用包可能需要。

**大小写拯救**:
核心文件仅因大小写错误不可读时，处理中改名修复而非删除。`pack.mcmeta` 大小写错（包不可读）→ 归嵌套（橙，可修）；`pack.png` 大小写错（仅图标丢失，包可读）→ 归臃肿（紫）。

**扩展名拯救**:
内容为 zip（PK 魔数）但扩展名不是小写 `.zip` 的文件（含 `.ZIP` 大写、假 `.rar` 后缀、无后缀——MC 的扩展名过滤大小写敏感）：按 zip 解析内部结构分类，可救则归嵌套（橙），处理时产物改正扩展名。魔数为准，扩展名只是提示。

**正常（绿）**:
zip 魔数 + 小写 `.zip` 扩展名 + 根层恰好三核心，无多余条目、无垃圾文件、未命中死路径，且无 **Lunar 非法转义**。无任何问题分组时的收纳位；也是处理的目标形态。

**文件夹（黄）**:
文件夹形态、根层有三核心的包。MC 能读，但非分享标准形态；处理 = 压为正常 zip。

**Lunar非法（冰青）**:
`pack.mcmeta` 含 **Lunar 非法转义** 的分组标签。数据病，与结构类正交：可与嵌套/文件夹/臃肿同时成立；仅当结构维为正常 zip 时，它是该包唯一的问题分组。处理时凡带此标签者产物 mcmeta 一律最小修补（保留颜色与原文）；若同时无其它可修结构病，则按纯 Lunar 包处理（原件入 problematic_packs → 产物回 resourcepacks）。用户可见名：zh-CN/zh-TW「Lunar非法」、en「Illegal in LC」。
_Avoid_: 与嵌套混为一谈、因有 § 颜色就贴此标签、把非法（红）与 Lunar 叠标

**非法（红）**:
无法识别为材质包也无法自动拯救的顶层条目：非 zip 魔数的文件、打不开/加密的 zip、递归到底找不到三核心的容器、嵌套超过 10 层的。处理时移入 illegal_packs 等候用户处置。真 RAR/7z 文件归此类，但结果中单独提示"请手动解压后重扫"（v1 仅支持 zip）。**互斥独占**：归非法的包不再属于其它任何分组（含 Lunar非法）。

**分组（多标签）**:
一个包可同时属于多个**分组**（结构病 + Lunar 数据病重合时）。结构维内部仍互斥：嵌套 / 文件夹 / 臃肿 / 正常 至多其一；**非法** 与一切其它分组互斥。Lunar非法 与嵌套/文件夹/臃肿可并存。六张卡片的计数按「带该标签的包数」独立累计，总和**可以大于**顶层包数。处理时每个包仍只处理一次，一次修完其所有标签对应的病。
_Avoid_: 强制单主类导致重合包从某张卡上「消失」、六计数之和必须等于包数、同一包因多标签被处理两次

**列表锚点序**（未过滤时列表排序用）:
包在列表中的位置按它所持标签中**最靠前**的展示位决定：非法 → 嵌套 → Lunar非法 → 文件夹 → 臃肿 → 正常。多标签包只出现**一行**（不重复占多行）。行上：**主色圆点**取锚点分组色；另含 Lunar非法 时名称旁加**冰青月亮徽章**（不堆第二大圆点）。
_Avoid_: 同一包在列表里复制多行、多标签时并排两个大圆点

**展示序**:
卡片与分组条的统一顺序：非法 → 嵌套 → Lunar非法 → 文件夹 → 臃肿 → 正常。非法置顶（仅用户可处置）；嵌套先于 Lunar非法（MC 结构不可读优先于 Lunar 数据不可读）；黄/紫为「能读但规范化」；正常收尾。

**卡片过滤**:
点击某张概览卡时，列表显示**含该分组标签**的全部包（标签过滤器，非锚点过滤器）。多标签包在其每一个所属卡下都能被筛到；例如点 Lunar非法 可见纯 Lunar、嵌套+Lunar、臃肿+Lunar。无过滤时列表仍按 **列表锚点序** 排成一行一条。
_Avoid_: 按锚点主位过滤导致重合包从某张卡列表中消失

**被占用（锁定预检）**:
确认处理前对待处理包做的只读探测：能否安全改名（申请删除权限打开不报共享冲突）。被占用通常因运行中的 MC 持有**当前选中**包的句柄；未选中的包不受影响。仅文件形态的包参与预检，文件夹包不预检（MC 按需开关文件，不持久持锁），处理时 `skipped_locked` 兜底。被占用的包在确认弹窗**置顶警告区**（黄底行）显示，可「重新检测」（仅重探锁，不重扫目录）；用户自行决定关闭 MC 或直接确认——被占用的包跳过，绝不强改。
_Avoid_: 用挂起/恢复进程绕锁（句柄是内核对象，挂起不释放，强改致崩溃）、按进程名扫描判定占用、把预检结果当处理时点的保证

**plot_temp**:
Plot 的工作目录，建在 exe 同目录下，内含 problematic_packs 与 illegal_packs。Plot 永不自动删除其中内容，等候用户处置。

**problematic_packs**:
plot_temp 下存放已处理包原件的目录。处理 = 原件移入此处 → 产物生成到 resourcepacks。任何失败时原件仍在此处可手动恢复。

**illegal_packs**:
plot_temp 下的非法条目隔离区。处理时非法（红）条目从 resourcepacks 移入此处。

**产物**:
处理生成的正常（绿）zip。写入 resourcepacks 时若与现存文件重名，自动加 " (1)" 递增后缀，绝不覆盖不同来源的文件；problematic_packs 内跨次运行重名同理。
_Avoid_: 静默覆盖

**嵌套（橙）**:
MC 当前读不了、但 Plot 可自动修复的包。成因：①容器套容器（主成因；文件夹与 zip 外壳等同，递归上限 10 层，超限归非法）②扩展名错误（魔数拯救）③pack.mcmeta 大小写/typo/缺失（mcmeta 拯救）。处理 = 解包/改名/生成，重压为正常包。
_Avoid_: 仅把压缩文件算作嵌套外壳

**mcmeta 拯救**:
pack.mcmeta 是三核心中唯一可合成的文件。两段拯救：先按内置 typo 模式改名（`pack.mcmeta.mcmeta` 等，保住作者原件）；改后仍缺失但有 assets 则生成默认件（pack_format=1，description 用包名）。

**合集包**:
一个嵌套包内并列存在多个含三核心的层。处理时全部拆出，各成一个独立正常包；找到三核心层后不再向其内部递归（内部子容器按多余条目处理）。

**Lunar 隐身（现象）**:
包不出现在 Lunar Client 材质列表。在现行 Lunar 上，可复现病灶是 `pack.mcmeta` 中的**非法 JSON 转义**（如 `\!`）；合法颜色码（`\u00A7…` 文本转义或字面 §）不是病因，修复时必须保留。对照：OTB/Mav 等同模板全隐，仅删非法反斜杠后（`\u00A7b\!` → `\u00A7b!`）立即可见。
_Avoid_: 把彩色简介当病因、认为刷 mtime 能治内容病、用旧版 Lunar（无此读取 bug）上的实验否定字节级病灶

**Lunar 非法转义**:
`pack.mcmeta` 内反斜杠后跟 JSON 不允许的字符（合法仅 `" \ / b f n r t` 与完整 `\uXXXX`）。最小修补 = 删除该反斜杠、保留其后字符与全部颜色/原文。产物重建一律过此修补；坏死到无法挽救时再走 **mcmeta 拯救**。
_Avoid_: 剥掉 §/颜色来「修好」、把合法 `\u00A7` 当非法转义

**Lunar 检测范围**:
在下列对象上读取判定用 mcmeta 并贴 **Lunar非法** 标签：① 根层 zip（含扩展名错误但魔数为 zip 者）的根层 mcmeta；② 根层文件夹包的根层 mcmeta；③ 嵌套容器内、与结构判定同一层的核心 mcmeta。读不到 mcmeta、或包已归 **非法** 时不检、不贴标签。
_Avoid_: 只扫顶层 zip 而漏文件夹/嵌套内层、对 zip 内任意深度所有 mcmeta 盲目扫描

**解包命名**:
嵌套包解出的产物一律用内层容器名（含三核心那一层的名字，即作者原名），不沿用外层文件名。
