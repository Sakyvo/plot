# Plot

Minecraft PotPvP 材质包修复工具（Tauri、仅 Windows、单文件 exe、Apache-2.0）。扫描 `resourcepacks` 目录，找出 MC 1.8 无法读取的材质包并自动修复为标准结构。

## Language

**判定基准**:
先用 **纹理目录版本信号**排除高版本包，再按 MC 1.8.x（PotPvP）能否实际读取与结构形态判定结构类（嵌套/臃肿/正常/非法）；zip 内部路径大小写敏感（`Pack.mcmeta` 对 MC 无效）。**Lunar非法** 另按 `pack.mcmeta` 是否含 **Lunar 非法转义** 判定（现行 Lunar 可读性），与 MC 结构维正交，可与结构类并存于同一包。
_Avoid_: 用 `pack_format`、mcmeta 可解析性或文件名猜版本、用旧版 Lunar 实验否定非法转义病灶、把 § 颜色当病因

**三核心文件**:
`assets` 文件夹、`pack.mcmeta`（必需）、`pack.png`（可选），位于包的根层级。MC 1.8 读取一个包所依赖的全部结构。

**候选核心层**:
已有 `assets` 文件夹的材质核心层；缺少或写错 `pack.mcmeta` 仍算候选并可走 **mcmeta 拯救**，完全找不到候选层才可能归 **非法**。

**包（待分类对象）**:
通常是 `resourcepacks` 目录的一个顶层条目；**分类文件夹**本身是收纳容器，其直接子级材质包各自成为待分类对象并保留相对位置。其它顶层文件夹的内部内容仍属于同一个包的内部结构。

**分类文件夹（黄）**:
自身不是材质包、且直接子级中至少含两个有效分类子项的文件夹；有效分类子项可以是独立材质包、递归成立的分类文件夹，或具有明确归档意图但检测后归非法的条目，说明文件、截图和垃圾文件不计入数量。它是内部包的父级收纳关系而非包的结构标签：黄色卡统计所有保留的分类文件夹节点，材质包总数只统计内部包；黄色卡仅用于查看收纳树，不代表问题、不触发处理，也不进入处理确认。可修产物回到原分类路径，原件备份与非法隔离镜像该分类路径。压缩外壳移除后显露的真实目录也按同一条件判断；非垃圾附件始终原样保留。分类文件夹内各包独立处理，一个包失败不回滚已成功的兄弟包。
_Avoid_: 因分类文件夹是文件夹形态就把它压成 zip、处理后把内部包移到 resourcepacks 根目录

**纹理目录版本信号**:
同一 **候选核心层**中，复数目录 `assets/minecraft/textures/items/` 或 `blocks/` 是低版本证据并优先；否则，单数 `item/` 下存在贴图文件，或存在单数 `block/` 目录，即视为高版本证据。空的单数 `item/` 不算证据；完全没有这些目录时保持原版式宽容，继续按结构分类。版本信号只作用于命中的内包本身，不向上传播到分类文件夹或其它外壳，也不影响同一容器中的其它内包。
_Avoid_: 用 `pack_format` 或 mcmeta 内容参与版本判定、让单数目录覆盖同层明确的复数低版本目录、因没有物品/方块贴图而忽略

**忽略（灰）**:
由 **纹理目录版本信号**确认可能属于高版本、超出 Plot 1.8 内容修复范围的内包。忽略包只属于灰色分组，不叠加结构类或 **Lunar非法**；Plot 不修改其内容或存储形态，但可移除包外的单包文件夹或压缩外壳，并将 ZIP 原字节、文件夹包原目录释放到应在位置。列表行与处理结果均显示忽略原因。
_Avoid_: 把忽略包归正常、非法或可修分类，修改忽略包内容、让忽略状态传播到上层容器或同容器的其它包

**忽略原因**:
用户可见的忽略依据：列出命中的单数 `item` / `block` 纹理路径，并提示“可能是高版本材质”；列表行与处理结果使用同一原因。
_Avoid_: 只给灰色圆点而不解释、把内部错误文本直接展示给用户

**已忽略（未处理）**:
处理结果末尾的灰色分组，合并列出本批次开始时已有的忽略包，以及处理后新分离并经重扫识别出的忽略包，同一路径只列一次；逐项显示 **忽略原因**，分离项另标父包来源，“复制结果”保留同样信息。

**臃肿（紫）**:
MC 能读、但需要规范化的包。纯结构判定，不设尺寸阈值。触发条件：①根层有三核心外的多余条目 ②命中死路径黑名单 ③pack.png 仅大小写/typo 错误（可改名拯救）④自身为 **文件夹包**。垃圾文件不触发分类；根层多余项若是 **误塞容器**，须分离而非删除。
_Avoid_: 按文件大小判定臃肿、按垃圾文件判定臃肿

**误塞容器**:
一个已有低版本/未知版本根核心的父包中意外出现的独立容器：ZIP / RAR / 7z 魔数或对应的不区分大小写扩展名命中的额外文件（即使损坏或加密），或在递归上限内能找到候选核心层的额外文件夹；其它归档格式不属本次范围。处理时只将最外层容器原样分离，重扫后独立分类：压缩文件保留原始字节，文件夹保留文件夹形态，本批次不递归修复；无法验证为材质包的压缩文件归 **非法**，父包仍按自身问题处理。分离目标重名时沿用 **产物** 的递增后缀且不覆盖，结果保留实际名称与父包来源；同一父包的转换与全部分离是一个原子操作，任一项失败则全部不提交并保留可恢复原件。
_Avoid_: 把误塞容器当普通冗余删除、因子容器是高版本而忽略整个父包、在分离当次递归修复未展示给用户的内包

**垃圾文件**:
操作系统/压缩工具产生的已知无用文件：`__MACOSX`、`.DS_Store`、`Thumbs.db`、`desktop.ini`。不触发任何分类（实测 85% 流通包内含，作信号则分类失效）；包因其他原因进入处理流程时顺手删除；出现在 resourcepacks 顶层时完全忽略。

**单包文件夹外壳**:
自身不是材质核心、仅含一个有效分类子项的文件夹。有非垃圾附件时保留目录并在原位处理内包；没有附件或只含垃圾文件时折叠该层，将内包移到上一级并移除空外壳。折叠是原子操作，内包未成功释放或修复时保留原外壳。
_Avoid_: 为折叠外壳而删除或移动说明文件、预览图等用户附件

**死路径**:
MC 任何版本都不会读取的 assets 内路径，代码内维护黑名单。v1 仅收 `assets/*/records/`（Yokabi 病：音乐碟塞错位置，正版路径是 `assets/*/sounds/records/`）。1.9+ 目录（advancements/recipes 等）不列入——双版本通用包可能需要。

**大小写拯救**:
核心文件仅因大小写错误不可读时，处理中改名修复而非删除。`pack.mcmeta` 大小写错（包不可读）→ 归嵌套（橙，可修）；`pack.png` 大小写错（仅图标丢失，包可读）→ 归臃肿（紫）。

**扩展名拯救**:
内容为 zip（PK 魔数）但扩展名不是小写 `.zip` 的文件（含 `.ZIP` 大写、假 `.rar` 后缀、无后缀——MC 的扩展名过滤大小写敏感）：按 zip 解析内部结构分类，可救则归嵌套（橙），处理时产物改正扩展名。魔数为准，扩展名只是提示。

**正常（绿）**:
未命中高版本 **纹理目录版本信号**，且为 zip 魔数 + 小写 `.zip` 扩展名 + 根层恰好三核心，无多余条目、无垃圾文件、未命中死路径，且无 **Lunar 非法转义**。无任何问题分组时的收纳位；也是处理的目标形态。

**文件夹包**:
文件夹形态、根层有三核心的单个材质包。MC 能读但非分享标准形态，归入 **臃肿**；它不是 **分类文件夹**。
_Avoid_: 把文件夹包计入分类文件夹分组

**Lunar非法（冰青）**:
`pack.mcmeta` 含 **Lunar 非法转义** 的分组标签。数据病，与结构类正交：可与嵌套/臃肿同时成立；仅当结构维为正常 zip 时，它是该包唯一的问题分组。处理时凡带此标签者产物 mcmeta 一律最小修补（保留颜色与原文）；若同时无其它可修结构病，则按纯 Lunar 包处理（原件入 problematic_packs → 产物回 resourcepacks）。用户可见名：zh-CN/zh-TW「Lunar非法」、en「Illegal in LC」。
_Avoid_: 与嵌套混为一谈、因有 § 颜色就贴此标签、把非法（红）与 Lunar 叠标

**非法（红）**:
无法识别或读取的顶层条目：非 zip 魔数的文件、打不开/加密的 zip、递归到底找不到候选核心层的容器、嵌套超过 10 层的。处理时移入 illegal_packs 等候用户处置。真 RAR/7z 文件归此类，但结果中单独提示"请手动解压后重扫"（v1 仅支持 zip）。**互斥独占**：归非法的包不再属于其它任何分组（含 Lunar非法与忽略）。

**分组（多标签）**:
一个包可同时属于多个**分组**（结构病 + Lunar 数据病重合时）。结构维内部仍互斥：嵌套 / 臃肿 / 正常 至多其一；**非法**、**忽略**各自与一切其它包分组互斥。Lunar非法 与嵌套/臃肿可并存。分类文件夹卡统计保留的分类节点；嵌套卡统计嵌套包与可移除外壳两类问题节点；其余卡统计对应包数。材质包总数只统计实际内包，各卡之和无需等于总数；处理时每个包仍只处理一次，一次修完其所有标签对应的病。
_Avoid_: 把分类文件夹当成内部包的互斥标签、强制单主类导致重合包从某张卡上「消失」、同一包因多标签被处理两次

**列表锚点序**（未过滤时列表排序用）:
包在列表中的位置按它所持标签中**最靠前**的展示位决定：非法 → 嵌套 → Lunar非法 → 臃肿 → 正常 → 忽略。多标签包只出现**一行**（不重复占多行）；分类文件夹按实际层级组成父行树，同一目录内先按问题严重度、再按名称排列，分类子目录按名称排列。包行上：**主色圆点**取锚点分组色；另含 Lunar非法 时名称旁加**冰青月亮徽章**（不堆第二大圆点）。
_Avoid_: 同一包在列表里复制多行、多标签时并排两个大圆点

**单内包外壳合并行**（仅列表展示）:
一条仅含一个可修内包（正常/嵌套/臃肿/Lunar非法）的外壳链在列表中合并为**一行**：渲染最外层外壳行，圆点取橙色嵌套色，无展开三角，内层行隐藏；内包的 Lunar 月亮徽章与忽略原因上浮到该行。忽略或非法内包、含附件或分类文件夹的外壳、多内包外壳保持原树形展开。合并只影响展示，引擎扫描分类、七卡计数与处理流程不变；外壳行点击打开/定位仍指向最外层外壳路径。
_Avoid_: 把合并行当成引擎分类变化、因合并而漏掉月亮徽章或忽略原因、合并忽略/非法内包

**展示序**:
卡片的统一顺序：非法 → 嵌套 → Lunar非法 → 分类文件夹 → 臃肿 → 正常 → 忽略。非法置顶（仅用户可处置）；嵌套先于 Lunar非法（MC 结构不可读优先于 Lunar 数据不可读）；黄色表示用户的收纳关系，紫色表示能读但需规范化；正常后紧跟灰色忽略。

**卡片过滤**:
点击包分组卡时，列表显示**含该分组标签**的全部包（标签过滤器，非锚点过滤器）；分类文件夹内的命中包保留祖先路径。点击分类文件夹卡显示完整目录树；点击嵌套卡同时显示外壳节点与真正的嵌套包。过滤或搜索激活时，文件夹默认展开以显露命中项，但**展开三角仍可手动折叠**；解除过滤后保留用户手动调整的展开状态。多标签包在其每一个所属包分组卡下都能被筛到；例如点 Lunar非法 可见纯 Lunar、嵌套+Lunar、臃肿+Lunar。
_Avoid_: 按锚点主位过滤导致重合包从某张卡列表中消失、过滤激活时强制锁死展开状态不让折叠

**被占用（锁定预检）**:
确认处理前对待处理包做的只读探测：能否安全改名（申请删除权限打开不报共享冲突）。被占用通常因运行中的 MC 持有**当前选中**包的句柄；未选中的包不受影响。仅文件形态的包参与预检，文件夹包不预检（MC 按需开关文件，不持久持锁），处理时 `skipped_locked` 兜底。被占用的包在确认弹窗**置顶警告区**（黄底行）显示，可「重新检测」（仅重探锁，不重扫目录）；用户自行决定关闭 MC 或直接确认——被占用的包跳过，绝不强改。
_Avoid_: 用挂起/恢复进程绕锁（句柄是内核对象，挂起不释放，强改致崩溃）、按进程名扫描判定占用、把预检结果当处理时点的保证

**plot_temp**:
Plot 的工作目录，建在 exe 同目录下，按处理批次收纳 **批次文件夹**。Plot 永不自动删除其中内容，等候用户处置。旧版本遗留的平铺 problematic_packs/illegal_packs 原地保留、不再被新批次复用。

**批次文件夹**:
一次处理批次的产物根：`plot_temp/Plot_YYYY-MM-DD_HH.mm.ss`（处理开始时刻的本地时间，冒号换点是 Windows 文件名限制），内含本批的 problematic_packs、illegal_packs 与 `.work` 暂存目录。同秒撞名按 ` (1)` 递增改名，绝不覆盖既有批次；批次之间同名单元天然隔离、不再加后缀。空批次（无可处理对象）不创建任何目录。

**problematic_packs**:
批次文件夹下存放本批已处理包原件的目录。处理 = 原件移入此处 → 产物生成到 resourcepacks；所谓删除或移除外层容器，也只表示将原件移入此处，不做永久删除。分类文件夹内包的原件镜像其分类路径，任何失败时均可按来源手动恢复。

**illegal_packs**:
批次文件夹下的本批非法条目隔离区。处理时非法（红）条目从 resourcepacks 移入此处；分类文件夹内的非法包镜像其分类路径。

**产物**:
处理生成的正常（绿）zip。分类文件夹内包的产物回写原分类路径；若与同一目标目录内的文件重名，自动加 " (1)" 递增后缀，绝不覆盖不同来源的文件。problematic_packs 与 illegal_packs 的镜像目录内同名仍自动加 ` (1)` 递增后缀（兜底）；跨批次不做去重——批次天然隔离。
_Avoid_: 静默覆盖

**嵌套（橙）**:
橙色问题节点，包括未命中高版本 **纹理目录版本信号**、MC 当前读不了但 Plot 可修复的嵌套包，以及必须移除的 **压缩外壳** / **单包文件夹外壳**。包的成因还包括扩展名错误与 pack.mcmeta 大小写、typo 或缺失；递归超过 10 层归非法。一个外壳与其内部真正的嵌套包分别计数。
_Avoid_: 仅把压缩文件算作嵌套外壳、把分类文件夹当单包外壳折叠

**压缩外壳**:
自身没有根层候选核心、只用于包裹一个或多个内包的压缩文件；它永不成为分类容器，处理时原件进入 **problematic_packs**，内包释放到其应在位置。根层已有候选核心的 ZIP 是真实材质包，其额外容器仍按 **误塞容器** 分离。内包先各自执行完整分类：正常 ZIP 与忽略 ZIP 保留原字节，低版本/未知版本问题包按对应规则修复，高版本文件夹包保持目录形态。内层真实目录递归判断：含至少两个有效分类子项者保留；只有一个有效分类子项且没有非垃圾附件的冗余层继续折叠。属于保留目录的非垃圾附件随目录释放；外壳根层散落附件不写入 resourcepacks，只保留在完整原件中并在处理结果说明。整个压缩外壳是一个原子事务：所有内包和目录结构准备成功后才一次性提交，真实读写失败则不产生半套结果并保留外层原件；内包归非法是正常分类，不算事务失败。
_Avoid_: 因压缩外壳内有多个包就保留外层 zip、无条件抹平内层真实分类目录

**mcmeta 拯救**:
`pack.mcmeta` 是三核心中唯一可合成的文件。先按固定优先级将大小写/typo 候选（如 `Pack.mcmeta`、`pack.mcmeta.mcmeta`）改为标准名并保留作者内容；仍缺失时生成 `pack_format = 1`、description 为包名的默认文件。已有精确 mcmeta 的 JSON 内容、`pack_format` 或 description 不触发分类或重写。
_Avoid_: 因 mcmeta 无法解析、缺字段或版本值不同而忽略，覆盖已有精确 mcmeta

**合集包**:
一个压缩外壳内并列存在多个内包。处理时移除外壳并逐个释放：低版本或未知版本内包继续分类与修复，高版本内包仅自身归 **忽略**；已有根核心的父包内额外容器属于 **误塞容器**，先分离而不参与父包版本判断。

**Lunar 隐身（现象）**:
包不出现在 Lunar Client 材质列表。在现行 Lunar 上，可复现病灶是 `pack.mcmeta` 中的**非法 JSON 转义**（如 `\!`）；合法颜色码（`\u00A7…` 文本转义或字面 §）不是病因，修复时必须保留。对照：OTB/Mav 等同模板全隐，仅删非法反斜杠后（`\u00A7b\!` → `\u00A7b!`）立即可见。
_Avoid_: 把彩色简介当病因、认为刷 mtime 能治内容病、用旧版 Lunar（无此读取 bug）上的实验否定字节级病灶

**Lunar 非法转义**:
`pack.mcmeta` 内反斜杠后跟 JSON 不允许的字符（合法仅 `" \ / b f n r t` 与完整 `\uXXXX`）。最小修补 = 删除该反斜杠、保留其后字符与全部颜色/原文；是否贴标与 `pack_format` 无关。产物重建一律过此修补，补丁后仍无法供 Lunar 读取时才走 **mcmeta 拯救**。
_Avoid_: 剥掉 §/颜色来「修好」、把合法 `\u00A7` 当非法转义

**Lunar 检测范围**:
在下列对象上读取判定用 mcmeta 并贴 **Lunar非法** 标签：① 根层 zip（含扩展名错误但魔数为 zip 者）的根层 mcmeta；② 根层文件夹包的根层 mcmeta；③ 嵌套容器内、与结构判定同一层的核心 mcmeta。读不到 mcmeta、或包已归 **非法** / **忽略** 时不检、不贴标签。
_Avoid_: 只扫顶层 zip 而漏文件夹/嵌套内层、对 zip 内任意深度所有 mcmeta 盲目扫描

**解包命名**:
嵌套包解出的产物用内层容器名（含三核心那一层的名字，即作者原名），不沿用压缩外壳或单包文件夹外壳名；满足条件的内层分类目录保留其名称与相对路径。
